ARG POSTGRES_VERSION='16'
ARG DB_NAME='postgres'
ARG DB_USER='root'
ARG DB_PASSWORD='default'

FROM postgres:${POSTGRES_VERSION:+${POSTGRES_VERSION}}
ENV POSTGRES_USER = ${DB_USER}
ENV POSTGRES_PASSWORD = ${DB_PASSWORD}
ENV POSTGRES_DB = ${DB_NAME}